Sprint 11 "The Hub Is a Rollup" (SPRINT-PM-11) ran to completion on 2026-09-06 in orchestrator run orch-2026-09-06-7e27: 36 tasks accepted, 6 stories closed, 21/21 points, 0 retries, 0 parks. Sprint 10 and Sprint 11 code and project state are BOTH still uncommitted.

Next time:
1. Commit. The working tree carries Sprints 10 and 11 together (src, tests, docs, .project). Review `git status`, then one code commit and one `pm:` commit for .project (pm_commit now takes prefix, not scope). Ten tracked files are deleted on purpose (five audit-era hub docs, five hub git test files).
2. Reinstall the server: `pipx install --force "/mnt/repos/ProjectMan[all]"`, then `projectman refresh-skills --keep-local`, then restart Claude Code. The live session's MCP tools still predate Sprint 10 (they still accept project=, scope=, and list pm_push_all/pm_repair/pm_validate_branches).
3. Close the epics: EPIC-PM-4 (all stories done once US-PM-33 closed) and EPIC-PM-5 (US-PM-31/34/35/36/37 all done). pm_update(status="done") on each.
4. Known failing tests on this tree, all pre-existing and not touched by Sprint 11: tests/test_orchestrate_design_doc.py (14), tests/test_orchestrate_skill_size.py (1), tests/test_migrate_archived_as_done.py::TestTheDocstringMatchesTheCode (1, click Sentinel default). Everything else passes (5234 passed at the last full run). Fix or accept them before the next sprint counts "full suite green".
5. Two human follow-ups still open from Sprint 10: migrate the real .project to gitignored derived indexes per docs/reference/file-formats.md; and, if this hub-shaped repo ever registers subprojects, run `projectman migrate-hub` once (it now also lifts subproject epics to the hub).
6. Leftovers worth a story: the web API still takes ?project= in src/projectman/web/routes/api.py get_store (recorded in ADR-003 Consequences); hub/dashboards.py generate_dashboards has no caller in src (cron.md now says so); search.py aborts on one malformed .md file. Backlog candidates: US-PRJ-55 (hub troubleshooting guide, now writable against the new docs), US-PRJ-34 and US-PRJ-41 (re-read against the 1128-line registry.py; likely moot).
