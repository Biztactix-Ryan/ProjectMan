Sprint 14 "Remove the Hub, Ship 0.9" (SPRINT-PM-14) completed 2026-09-08 by run orch-2026-09-08-8293: 21/21 points, 35/35 tasks accepted (12 dispatches, 23 verify tasks accepted on orchestrator-run evidence), 0 retries, 0 parks, 5 stories closed (US-PM-44, US-PM-45, US-PM-46, US-PM-47, US-PM-42). Hub mode is gone: no prefix parameter on any tool or web route, src/projectman/hub deleted, hub CLI commands and --hub/--project/--prefix options gone, ProjectConfig has no hub/projects fields (legacy config.yaml keys are ignored with a one-line stderr warning), docs/hub-mode deleted, ADR-004 supersedes ADR-003, CHANGELOG has [0.9.0] - 2026-09-08 and pyproject.toml says 0.9.0. Final suite: 5015 passed, 18 skipped, 0 failed (tests/integration still env-blocked). `projectman git-status` was rewired to the one store (worktree.store_status_payload) rather than deleted.

Next time:

1. NOTHING IS COMMITTED. The tree now carries Sprints 12, 13 AND 14 (code: ~81 modified + 23 deleted + 2 new test files; .project: ~250 files). Commit code first, then a `pm:` commit for .project. This repo's .project/config.yaml still carries hub: false / projects: [] — the next PM write through save_config will drop them silently; that diff is expected.

2. Release steps, in order, after the commit: `pipx install --force "/mnt/repos/ProjectMan[all]"`, `projectman refresh-skills --keep-local` (templates changed in US-PM-47-4; the repo-tracked .claude/skills/pm/SKILL.md and .claude/agents/pm.md were already regenerated by the orchestrator and match the templates), restart Claude Code, then `git tag v0.9.0` on the release commit and push — the CHANGELOG compare links already point at v0.9.0.

3. Two behaviour notes from the removal worth a look: (a) single-store pm_context no longer returns VISION/ARCHITECTURE at all — they were only ever emitted from a hub root as hub_vision/hub_architecture; if pm_context should surface the project's own VISION.md/ARCHITECTURE.md that is a small new story; (b) EPIC-PM-6 (Remove hub mode) still reads active — close it with pm_update(status="done") once the commit lands.

4. Backlog before planning Sprint 15: US-PRJ-60 (pm_tags/pm_rename_tag/pm_export) and US-PM-22 (API auth stub) still need a product call; the story-with-archived-todo-tasks auto-close bug is the only new candidate; EPIC-PRJ-6 is the other epic still active.

5. Known env-blocked: tests/integration (mcp 2.x in .venv). Runner: uv run --extra dev --with "mcp[cli]<2" --with fastapi --with httpx --with numpy python -m pytest tests -q -p no:cacheprovider --ignore=tests/integration.
